def reverse(text):
  rev = []
  index = len(text)
  while index:
    index -= 1                       
    rev.append(text[index])
  return ''.join(rev)
  
  
# Extended Slice
reverse[::1]
# esrever
  
''.join(reversed('reverse'))
# esrever
